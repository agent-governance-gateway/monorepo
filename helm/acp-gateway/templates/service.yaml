apiVersion: v1
kind: Service
metadata:
  name: {{ include "acp-gateway.fullname" . }}
  labels:
    {{- include "acp-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acp-gateway.selectorLabels" . | nindent 4 }}
